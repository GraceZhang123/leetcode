把第二个数组的元素追加到第一个数组尾部，然后直接Arrays.sort

class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        for (int i = 0; i < n; i++) {
            nums1[m  + i] = nums2[i];
        }

        Arrays.sort(nums1);
        
    }
}

